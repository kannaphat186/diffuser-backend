# NOTE: dotenv in this project loads src/.env (path is resolved against
# src/server.js via __dirname). Use src/.env.example as the canonical
# template. This file is kept only as a convenience mirror.
#
# Real loaded path: src/.env
# Template:         src/.env.example
